<%= simple_form_for [:admin,@job] do |f| %>
  <%= f.input :title,label:"your novel name"%>
  <%= f.input :description,placeholder:"tell more details"%>
  <%= f.button :submit, class:"btn btn-primary"%>
<% end %>
