<h3>投放简历到<%= @job.title %></h3>

<%= simple_form_for [:job,@resume] do |f| %>
  <%= f.input :content, label:"内容简介"%>
  <%= f.input :attachment,label:"上传简历" %>
  <%= f.submit "送出", class:"btn btn-primary"%>
<% end %>
